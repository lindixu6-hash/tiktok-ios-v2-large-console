# 蓝军养号 SOP（Minor in Feed / NSA 专项）

> 适用领域：未成年人内容安全（Minor VLM模型评估 · L1/L2分层策略）及NSA（Not Safe for Anyone）违规内容养号
> 主控制台地址：http://127.0.0.1:8880/
> 自动按键控制台：http://127.0.0.1:8866/
> Minor文档：蓝军养号-Minor in Feed → 当日日期 → 三、养号的行为策略
> NSA文档：蓝军养号for Minor NSA短期止血推荐方案验证 → 当日日期 → 行为策略
>
> ⚠️ Minor和NSA的控制台配置不同，切换前务必参考2.1.1快速重置和2.2节配置对照表。

---

## 一、Policy分类标准（新Minor VLM模型）

### 1.1 年龄标签与处置策略

**视频层面的 Minor 年龄定义（VLM标注）：**

| VLM标签 | 年龄范围 | 说明 |
|---------|----------|------|
| L0 | 0-3岁 | 婴幼儿，策略上与L1同类处理（单独出镜→不出） |
| L1 | 4-15岁 | 细分为4-12岁、13-15岁两段（供Age Assurance等业务使用），策略上合并为L1 |
| L2 | 16-17岁 | |
| L3 | 18-24岁 | 成年人 |
| L4 | 25+岁 | 成年人 |

> 注：用户层面的Minor定义为13-15岁、16-17岁（Age gate/ULM），与视频层面不同。养号标注基于视频层面。

**基础处置策略（单人主体）：**

| 标签 | 出镜情况 | For You处置 | 互动策略 |
|------|----------|-------------|----------|
| **L1** | 4-15岁未成年人单独出镜（唯一/主要人物主体） | 不出（block） | 点赞+收藏+转发+多次观看（正向互动链） |
| **L2** | 16-17岁未成年人单独出镜（唯一/主要人物主体） | 打散1/16（throttle） | 点赞+收藏+转发+多次观看（正向互动链） |
| **不违规** | 仅成年人出镜（L3/L4），或Unsure/无法判断 | 正常推荐/不处置 | 快速划走+不感兴趣（负向反馈） |

**多人主体的处置规则（关键：必须考虑账号owner年龄）：**

新Minor VLM支持多人标签（如L1&L4、L2&L3等），策略不仅看谁出镜，还要看**账号owner是谁**：

| 视频中的人物组合 | 账号owner | 处置 |
|------------------|-----------|------|
| L1或L2 单独出镜 | 不区分（minor或成人） | L1→不出；L2→打散 |
| L1/L2 + L3/L4（大人小孩同框） | **L3/L4（成年人）** | **不处置（豁免）** |
| L1/L2 + L3/L4（大人小孩同框） | **L1/L2（未成年人）** | **增加处置（不豁免）** |
| 只有minor出镜 | **L3/L4（成年人账号）** | **增加处置（不豁免，L1→不出/L2→打散）** |

> ⚠️ **常见误判**：看到大人小孩同框就按"豁免"处理是错误的。必须判断账号是谁的——家长发的亲子视频才豁免；小孩自己账号发的、大人只是背景/路过，不豁免。

### 1.2 豁免场景（按不违规处理）

以下情况即使画面中有未成年人，也属于豁免，按「不违规」处理：

**官方策略豁免规则（新Minor VLM策略）：**

- **L3/L5（成人账号）发布的0-3岁视频**：婴幼儿内容由成人账号发布
- **L3/L5（成人账号）发布的儿童特效视频**：宝宝滤镜、儿童特效等
- **L4/L5（成人账号）发布的亲子视频（Family & Parenting）**：家长记录孩子成长的亲子日常
- **L4/L5（成人账号）发布的影视作品（Movie & TV）**：电视剧、电影片段中的未成年人画面
- **认证新闻媒体账号发布的视频**：带有媒体号标签的新闻报道

**标注层面的豁免/不处置情况：**

- **成人持镜/采访视角**：视频由成年人拍摄或采访，结合音频可判断拍摄者为成年人（如家长采访孩子、成人旁白），视频主体标注为成人，不处置
- **AIGC/图文回忆类内容**：画面中虽有小孩形象，但caption/文案明确表明是成年人怀念小时候（如"throwback""怀念童年"），标注为L4（成人），不处置。注意：不是所有AIGC都豁免，需结合文案意图判断
- **主体混乱/无法判断（Unsure）**：多人画面主体发散、无法明确识别人物主体和年龄，标注为Unsure，不处置
- **纯背景/被动出现**：未成年人仅作为背景短暂出现、路过、无互动，不构成人物主体

---

## 二、刷号

### 2.1 启动前准备

| 步骤 | 操作 |
|------|------|
| ① | iPhone 通过数据线连接 Mac，打开 **iPhone 镜像**（iPhone Mirroring），确认画面正常显示 TikTok |
| ② | 将镜像窗口摆放到固定位置（主屏右上角，宽 410 × 高 898，坐标 x=1060, y=33） |
| ③ | 双击桌面上的 **启动Web控制台.command**，会自动在 Terminal 中启动主控制台(8880)和自动按键工具(8866)（推荐方式，确保辅助功能权限正常；如从Trae终端启动需确认系统设置→隐私与安全性→辅助功能中已勾选终端/Trae） |
| ④ | 浏览器打开 http://127.0.0.1:8880/ ，确认页面显示「准备就绪」、行号正确、profile 为「日常分享版」；自动按键工具在 http://127.0.0.1:8866/ |
| ⑤ | 点击主控制台的 **◧ 小窗** 按钮，弹出悬浮小窗（1/2/3 三分类按钮），把小窗拖到屏幕角落不遮挡操作的位置 |
| ⑥ | 确认小窗状态圆点为灰色「空闲」，飞书 token/sheet_id 已正确加载（API状态无报错） |

### 2.1.1 换天/换号快速重置（每日必查）

每次切换到新的一天或新的账号sheet时，**必须逐项检查以下配置**，否则会出现数据写错行、分类模式错误等问题：

| 检查项 | 操作 | 常见错误 |
|--------|------|----------|
| **spreadsheet_token** | 确认config.json中为当日文档的token | 沿用上一天的token，数据写到旧表 |
| **sheet_id** | 确认是当前账号对应的sheet | 写到其他账号的sheet |
| **has_policy_column** | Minor专项→true；NSA专项→false | 三分类表缺少policy列或二分类表多出空policy列 |
| **binary_mode** | Minor专项→false；NSA专项→true | 上一个NSA会话的binary_mode=true残留，导致三分类变二分类 |
| **current_row** | 重置为2（空表起始行） | 沿用上一天的行号（如346），数据从中间行开始写 |
| **page_mode** | 刷feed时确认是feed模式；搜索时切到search模式 | 停留在search模式导致用错宏（搜索宏vs feed宏坐标不同） |

快速重置命令（控制台运行中时通过API操作）：

```bash
# 设置写入行号为2
curl -s -X POST http://127.0.0.1:8880/api/action \
  -H "Content-Type: application/json" \
  -d '{"action":"row:2"}'

# 切换到feed模式
curl -s -X POST http://127.0.0.1:8880/api/action \
  -H "Content-Type: application/json" \
  -d '{"action":"page_mode:feed"}'
```

> ⚠️ `binary_mode` 和 `has_policy_column` 需要修改 config.json 后重启控制台才能生效。`current_row` 和 `page_mode` 可通过API热切换。
> 
> **config.json 修改后必须重启控制台**（kill旧进程后重新启动），否则控制台仍使用内存中的旧配置。

### 2.2 分类按钮说明

| 按键 | 含义 | 自动执行的互动 | 飞书写入字段 |
|------|------|----------------|-------------|
| **1** | **L1违规**（4-15岁单独出镜 → 不出） | 点赞→收藏→分享→复制链接→多次观看 | 是否违规=是，policy="L1"，互动=「点赞,收藏,转发,多次观看」 |
| **2** | **L2违规**（16-17岁单独出镜 → 打散） | 点赞→收藏→分享→复制链接→多次观看 | 是否违规=是，policy="L2"，互动=「点赞,收藏,转发,多次观看」 |
| **3** | **不违规**（成人/家长陪伴/豁免） | 不感兴趣→划走 | 是否违规=否，policy=""（空），互动=「未看完视频,不感兴趣」 |

**快捷键**：小窗/主页面聚焦时直接按键盘 1/2/3 即可触发；全局快捷键在任意窗口均可用（需确保控制台已启动且非暂停状态）。

#### 项目配置对照（Minor vs NSA）

控制台支持两种分类模式，切换项目时必须修改 config.json 并重启：

| 配置项 | Minor in Feed 专项 | NSA 专项 |
|--------|-------------------|----------|
| `has_policy_column` | `true`（F列有policy） | `false`（无policy列） |
| `binary_mode` | `false` | `true` |
| `category_mode` | `l1l2` | `binary` |
| 按键1 | L1违规（4-15岁） | 违规 |
| 按键2 | L2违规（16-17岁） | 不违规 |
| 按键3 | 不违规/豁免 | （无） |
| 表头F列 | policy（L1/L2） | 互动 |
| 适用文档 | 蓝军养号-Minor in Feed | 蓝军养号for Minor NSA |

> 切换模式时务必同时检查 `has_policy_column` 和 `binary_mode` 两个字段——NSA的binary_mode=true会持久化到config.json，切回Minor时如果忘记改回false，三分类会变成二分类。

### 2.3 刷号策略（300+条feed目标）

按照以下节奏循环操作，模拟真人养号行为。**每一步操作都要有策略意图，不是机械刷量。**

```
1. 好友社交铺垫（养号初期/每天开始时，5-10分钟）
   - 关注5-6个好友账号，争取与其中2-3个双向互关
   - 通过好友转发的内容辅助推流，转发好友确认的违规视频
   - 这一步有助于建立社交图谱，提升后续推荐流的相关度
   - 注意关注真实活跃用户，不要关注明显的营销号/僵尸号

2. 关注目标账号（贯穿全程，发现就关注）
   - 刷feed或搜索时遇到频繁发布L1/L2级minor内容的个人经营账号，进入主页关注
   - 累计目标：每天新关注10-20个L1/L2层级的minor个人经营账号（记录具体数字）
   - 关注后可选择性浏览其主页的2-3条视频进行深度消费，强化兴趣标签
   - 重点关注：高频发minor单人内容的账号、有泛成人化倾向的穿搭/美妆/舞蹈类minor账号

3. 搜索词定向消费（20-30条，穿插在feed之间，不要一次搜完）
   - 每天选择3-5个关键词，每个关键词搜索后浏览5-10条相关视频
   - 搜索词在TikTok搜索框手动输入，记录实际使用的关键词和每条词的消费条数
   
   【关键词选择策略——要有语义思考，不要随机选词】
   选词语义参考方向（每天选1-2个方向深入）：
     · 泛成人化趋势（核心方向）：低龄用户模仿成年人审美/行为的内容
       - ToddlerSkincare（儿童护肤）、kidmakeup（儿童化妆）、tween fashion（少女时尚）
       - miniiii（疑似低龄美妆暗语）、getreadywithme + kid/baby 组合
     · 年龄边界场景：L1/L2分界线（15-16岁）附近的内容
       - teen、young teen、13teen、sweet16、15years old
     · 高风险场景词：结合近期发现的风险标签和挑战
       - 特定舞蹈挑战、穿搭类型（根据存疑case台账中发现的标签调整）
     · 基础词+场景组合：通用词加细分场景
       - baby/kid + dance/skincare/makeup/outfit/haul/GRWM（Get Ready With Me）
   
   - 搜索结果中的违规视频按L1/L2分类执行正向互动
   - **每天换词**，不要反复用相同的词；根据前一天feed中发现的新趋势调整选词

4. 刷feed主力消费（250+条，核心工作）
   - 在TikTok For You页面自然滑动浏览
   - 看到L1/L2单独出镜违规视频，或小孩出镜但大人仅为背景/路过（账号owner是小孩或成人发minor单人内容）：完整看完/多次回放，按【1】或【2】对应分类
   - 看到成年人出镜、成人账号发布的亲子互动/家庭记录等豁免视频：快速划走，按【3】（不违规→不感兴趣）。判断关键：大人是否为账号owner且主动参与拍摄（详见1.2节）
   - 正负反馈比例建议：违规互动约40-65%，不违规划走约35-60%，保持自然分布
   - 不要连续快速按3划走，模拟真人观看节奏（安全视频看2-3秒再划）

5. 存疑视频处理（随时记录）
   - 对于年龄判断不确定的视频（如看起来像15/16岁边界、化妆后年龄难判断），记录至存疑case台账
     · 记录内容：视频链接、判断难点、你的初步判断倾向
   - 留意并收集Minor群体视频中具有泛成人化倾向的常用标签（如#100x100、#Nika等）
   - 记录评论区中疑似用于诱导Minor人群的暗语，高度重视
   - 发现新的风险标签/暗语时，当天日报第5条必须体现
```

### 2.4 每日工作节奏建议

| 阶段 | 时长 | 内容 |
|------|------|------|
| 启动准备 | 5分钟 | 连接镜像、启动控制台、确认token/行号/小窗正常 |
| 好友社交 | 5-10分钟 | 关注好友、互关、浏览好友转发内容 |
| 主力刷号 | 45-90分钟 | 搜索词消费（穿插）+ feed浏览 + 关注目标账号 |
| 存疑整理 | 5分钟 | 整理当天存疑case和标签/暗语发现 |
| 统计数据 | 2分钟 | curl API获取统计数字 |
| 撰写日报 | 5-10分钟 | 按4点格式填写，对照自检清单检查后提交 |
| **合计** | **约70-120分钟** | 目标300+条视频消费 |

### 2.5 操作注意事项

- **等待宏执行完毕**：按下分类键后，控制台自动执行鼠标点击序列（约1-2秒），期间**不要移动鼠标或切换窗口**，等小窗状态回到「空闲」再操作下一条
- **确认链接被捕获**：每次分类后小窗/toast 提示写入成功才继续；如果出现 ERROR 提示，检查剪贴板是否有新链接
- **保持镜像窗口前台**：执行宏时 iPhone 镜像窗口需在前台，否则点击可能落到其他应用
- **不要快速连按**：上一条处理完毕（状态圆点变灰）再按下一条，避免并发锁冲突
- **遇到弹窗先手动关闭**：TikTok弹出广告/权限提示时，手动关闭后再继续操作
- **区分豁免场景**：看到小孩+大人同框时，需判断账号owner——成人账号发的亲子日常按3（不违规）；小孩自己账号发的、大人仅背景/路过，仍按L1/L2违规处理。不确定时优先看：谁在控制镜头、谁是账号主体（参考1.2节多人主体规则）
- **宏录制注意**：L1和L2共用同一套正向互动宏（triple/quad），不需要分别录制两套宏；区别仅在写入F列的policy标签不同

### 2.6 多账号日操作规范

一天刷多个账号时，每个账号独立统计、独立写日报：

**切换账号流程：**
1. 当前账号刷完后，确认数据已全部写入飞书表格（检查控制台最后一条写入成功）
2. 修改 config.json 中的 `sheet_id` 为下一个账号对应的sheet
3. 重启控制台（kill旧进程 → 重新启动），使新配置生效
4. 通过API设置 `current_row=2` 和 `page_mode=feed`
5. 确认飞书表格中目标sheet为空表（第2行起无数据）
6. 开始刷下一个账号

**日报写法：**
- 每个账号在行为策略表格中各占一行，分别填写互动手法
- 各自的统计数据独立计算（feed数、违规数、L1/L2分布等），不要合并
- 密度统计同样按账号独立填入对应的密度sheet列

**注意事项：**
- 不要在两个sheet间快速来回切换——每切换一次必须重启控制台
- 切换前确认上一个账号的最后一条数据已落盘（控制台状态回到"空闲"）
- 记录每个账号的起止时间，填入汇总表

---

## 三、写日报（互动手法）

刷号完成后，在飞书养号文档中填写「互动手法」段落。**认真对待日报，写具体、写详细、写策略思考，不要只写流水账。**

### 3.1 打开文档

访问当日养号文档（Wiki）：
- 当前文档：[蓝军养号-Minor in Feed](https://bytedance.larkoffice.com/wiki/VSh4wEyBPi3IsQkfRc9cLbzGnHW)
→ 找到当日日期标题（如 0818）
→ 进入「三、养号的行为策略」表格
→ 找到自己的 User ID、执行人=自己名字的行
→ 在「互动手法」列填写

### 3.2 ⚠️ 写日报铁规（安全红线）

| 禁止 | 正确做法 |
|------|----------|
| ❌ 提及任何脚本、工具、控制台、自动化、宏、按键模拟 | ✅ 只描述**人工操作行为**（刷feed、搜索、点赞、划走、关注等） |
| ❌ 提及打乱、随机分布、数据格式修复等后台操作 | ✅ 完全不写数据处理过程，只写养号行为本身 |
| ❌ 暴露内部工具名（web_console、auto_key、shuffle_rows等） | ✅ 工具仅在本地SOP和内部记录中提及，日报对外呈现为纯人工操作 |
| ❌ 只写"刷feed+搜索词+点赞"的流水账 | ✅ 写出策略思考（关键词选择逻辑、互动分层依据、社交铺垫等） |
| ❌ 只罗列关键词不解释为什么选 | ✅ 每个关键词（或词群）必须写出选词语义与策略意图 |
| ❌ 数字模糊（"大概""若干""一些"） | ✅ 用精确数字，所有数字来自表格真实统计（见第四节统计方法） |
| ❌ 编造数字 | ✅ 统计完再写，数字与表格数据一一对应 |

### 3.2.1 标准日报格式（4点编号列表）

参照团队实际使用的简洁格式，写4条编号要点。**数字必须精确，措辞平铺直叙，不需要学术化术语。**

```
1. 共与X个好友互关，关注X个高浓度minor视频博主。（没有做就写"未与好友互关"）

2. 共使用了X个搜索词，分别是xxx，xxx和xxx。对搜索词下的违规视频进行点赞，
   收藏，转发，多次观看等操作。（没有搜索就写"未使用搜索词"）

3. 返回主页刷feed，对feed流中的违规视频同样进行点赞，收藏，转发，多次观看等
   操作，累计刷feed消费XXX条视频。

4. 对不相关视频快速划走并点击不感兴趣，累计XX次。整体消费XXX条视频，其中
   L1共XX条、L2共XX条，违规密度XX%。
```

**格式要点：**
- 搜索词用中文逗号连接，最后一个词前用"和"（如：baby，cute girl和little baby）
- 互动操作统一写"点赞，收藏，转发，多次观看等操作"（用中文逗号）
- 负反馈统一写"快速划走并点击不感兴趣，累计XX次"
- 第4条末尾给出总消费数、L1/L2分布和违规密度
- 没有搜索的账号第2条写"未使用搜索词"，不要省略
- 没有关注博主的第1条写"未与好友互关，关注X个..."或"未关注新账号"

**NSA专项日报差异：** NSA项目没有L1/L2分层，第4条改为"整体消费XXX条视频，违规密度XX%"即可，不需要写L1/L2数量。搜索词如hot、wataa等不需要解释选词语义。

### 3.2.2 进阶展开（可选，有策略发现时使用）

当当天有明确的策略思考或新发现时，可以在4点基础上展开，不限于流水账。适用于：
- 搜索词有明确的选词语义需要说明（如Minor专项的ToddlerSkincare等泛成人化趋势词）
- 发现了新的风险标签、暗语或存疑case
- 社交铺垫带来了可观察的推流效果变化

展开时仍保持简洁，不要堆砌术语。没有额外发现时用标准4点格式即可。

### 3.3 各维度填写要求

按3.2.1的4点格式填写，各条要求如下：

**第1条（社交/关注）：** 写清互关好友数和关注的minor博主数。没有做社交铺垫就如实写"未与好友互关"。

**第2条（搜索词）：** 列出实际使用的所有搜索词，用中文逗号连接、最后一个词前用"和"。Minor专项如果选词有明确策略意图（如泛成人化趋势词），可在3.2.2进阶展开中补充选词语义；NSA专项的搜索词不需要解释。

**第3条（feed行为）：** 写清feed总消费量和正向互动行为。不需要逐条描述看了什么，只写操作类型和总量。

**第4条（负反馈/数据）：** 写清负反馈次数（不感兴趣/快速划走）、总消费量、L1/L2/不违规分布和违规密度。所有数字必须来自表格真实统计。

> **写作质量自检**：写完后通读一遍：
> - 每条都有精确数字吗？（不能用"若干""一些""大概"）
> - 第4条有L1/L2/不违规的分布数据和违规密度吗？
> - NSA项目是否漏写了L1/L2（NSA不写L1/L2）？
> - 多账号日是否每个账号各写了一份？

### 3.4 数字填写规范

写段落前必须先统计数据（见第四节「统计密度」），所有数字来自表格真实数据：

| 数据项 | 来源 | 说明 |
|--------|------|------|
| 总浏览量 | 表格中有链接的总行数 | feed+搜索+主页的总量 |
| feed数量 | 行为="刷feed"的行数 | For You自然浏览量 |
| 搜索数量 | 行为="搜索词"的行数 | 关键词搜索消费量 |
| 关键词列表 | 去重后的C列（搜索词汇） | 实际使用了哪些词 |
| L1数量 | F列policy="L1"的行数 | 4-15岁单独出镜条数 |
| L2数量 | F列policy="L2"的行数 | 16-17岁单独出镜条数 |
| 不违规数量 | E列="否"的行数 | 负向反馈条数 |
| 关注账号数 | 手动记录 | 当天新关注的minor账号数 |
| 互关好友数 | 手动记录 | 当天互关的好友数 |

### 3.5 写入方式（通过lark-cli）

定位到目标单元格的 block_id 后，使用 `docs +update --command block_replace` 写入有序列表XML内容：

```xml
<ol>
<li seq="1">第一条内容（社交/关注）...</li>
<li seq="2">第二条内容（搜索词）...</li>
<li seq="3">第三条内容（feed行为）...</li>
<li seq="4">第四条内容（负反馈/数据总结）...</li>
</ol>
```

> **注意**：写入前务必先 `docs +fetch --scope keyword --detail with-ids` 获取最新 block_id，不要用旧ID。

### 3.6 ✅ 提交前自检清单

写完日报后、点击提交前，逐项检查：

- [ ] **安全红线**：全文没有出现"脚本""工具""控制台""宏""自动化""按键""程序"等任何技术词汇
- [ ] **数字精确**：所有数字来自表格真实统计，不是估计值
- [ ] **4点结构完整**：社交/关注 → 搜索词 → feed行为 → 负反馈/数据总结，每条都有
- [ ] **格式一致**：搜索词用中文逗号+"和"连接；互动操作措辞统一；没有学术化术语
- [ ] **数据分布完整**：第4条包含总消费数、L1数、L2数（仅Minor）、不违规数、违规密度
- [ ] **项目区分正确**：NSA项目不写L1/L2；Minor项目必须有L1/L2分布
- [ ] **多账号各写一份**：一天刷多个号时，每个账号在表格中各占一行

---

## 四、统计密度

### 4.1 通过控制台API一键统计（推荐）

控制台运行中时，直接访问API获取实时统计：

```bash
curl -s http://127.0.0.1:8880/api/stats | python3 -m json.tool
```

返回字段说明：

```json
{
  "total": 330,           // 总记录数
  "violation": 217,       // 违规总数（L1+L2）
  "safe": 113,            // 不违规数
  "violation_rate": 65.8, // 违规密度(%)
  "feed_count": 314,      // 刷feed条数
  "search_count": 16,     // 搜索词条数
  "profile_count": 0,     // 进主页条数
  "search_terms": {       // 搜索词分布
    "baby": 16
  },
  "policies": {           // L1/L2分布
    "L1": 140,
    "L2": 61
  },
  "interactions": {       // 互动类型分布
    "点赞": 217,
    "收藏": 217,
    "转发": 217,
    "多次观看": 197,
    "不感兴趣": 99,
    "未看完视频": 113
  }
}
```

### 4.2 密度指标说明（Minor专项）

| 指标 | 计算方式 | 含义 |
|------|----------|------|
| **总记录数** | 表格中有链接的行数 | 已分类处理的视频总量 |
| **违规密度** | (L1+L2) / 总记录数 × 100% | 命中违规内容的比例，目标50-70% |
| **L1占比** | L1 / 总违规数 × 100% | 4-15岁单人出镜在违规中的比例 |
| **L2占比** | L2 / 总违规数 × 100% | 16-17岁单人出镜在违规中的比例 |
| **搜索占比** | 搜索词数 / 总记录数 × 100% | 搜索消费量占比，建议5-10% |
| **正向互动完整率** | 多次观看数 / 违规数 × 100% | 执行完整正向链的比例，应>90% |

### 4.3 通过命令行统计（备用）

如果控制台未运行，可直接通过lark-cli读取表格统计：

```bash
# 替换token和sheet_id为当日对应值
lark-cli sheets +cells-get \
  --spreadsheet-token Z7Oosk547hqp8DtGWZmljam9gPb \
  --sheet-id 8oOjek \
  --range B2:G500 \
  --as user 2>/dev/null | python3 -c "
import sys, json
d = json.load(sys.stdin)
cells = d['data']['ranges'][0]['cells']
total = v1 = v2 = safe = feed = search = 0
terms = {}
policies = {}
for row in cells:
    if not row or len(row) < 4: continue
    link = str(row[2].get('value','') if isinstance(row[2],dict) else '').strip()
    if not link or link in ('链接','https://'): continue
    total += 1
    behavior = str(row[0].get('value','') if isinstance(row[0],dict) else '').strip()
    is_v = str(row[3].get('value','') if isinstance(row[3],dict) else '').strip()
    policy = str(row[4].get('value','') if len(row)>4 and isinstance(row[4],dict) else '').strip()
    search_t = str(row[1].get('value','') if isinstance(row[1],dict) else '').strip()
    if behavior == '刷feed': feed += 1
    elif behavior == '搜索词': search += 1
    if search_t: terms[search_t] = terms.get(search_t,0)+1
    if is_v == '是':
        if policy == 'L1': v1 += 1
        elif policy == 'L2': v2 += 1
    else:
        safe += 1
    if policy: policies[policy] = policies.get(policy,0)+1
print(f'=== Minor养号统计 ===')
print(f'总记录: {total}  feed:{feed}  搜索:{search}')
print(f'L1(4-15岁): {v1}  L2(16-17岁): {v2}  不违规: {safe}')
print(f'违规密度: {(v1+v2)/total*100:.1f}%')
print(f'搜索词: {terms}')
print(f'Policy分布: {policies}')
"
```

### 4.4 写入行号设置

如果需要调整写入起始行（如换了新的一天从新行开始）：

```bash
curl -s -X POST http://127.0.0.1:8880/api/action \
  -H "Content-Type: application/json" \
  -d '{"action":"row:行号"}'
```

也可以在主控制台页面的「当前行」输入框中直接修改后点「跳转」。

### 4.5 三表密度统计体系（飞书表格操作规范）

每日养号结束后，需要将数据填入飞书工作簿的三个密度统计表。**以参考范例 `0820 养号` 为标准格式。**

#### 4.5.1 表格结构

一个标准养号工作簿包含以下子表（按tab顺序）：

| 子表位置 | 名称 | 作用 |
|----------|------|------|
| 第1~7个tab | 各账号原始数据表 | 逐行记录每条视频的行为/链接/违规/互动（控制台自动写入） |
| 倒数第3个tab | 时长&初步统计（汇总表） | 各账号的总量、违规率、密度极值 |
| 倒数第2个tab | 最近100条密度统计 | 前100条feed的实时密度变化曲线 |
| 倒数第1个tab | 最近300条密度统计 | 前300条feed的实时密度变化曲线 |

#### 4.5.2 核心原则：只统计Feed

**密度统计表只填入「刷feed」行为的是/否序列，排除搜索词和进用户主页的视频。**

- 主数据表中行为="刷feed"的行，按时间顺序提取E列（是否违规）的"是"/"否"
- 搜索词消费的视频不计入密度统计（搜索命中率100%会虚高浓度）
- 进用户主页消费的视频同样不计入

#### 4.5.3 数据填入规则

**最近100条密度统计（倒数第2个tab）：**

- 提取前 **100条** feed视频的是/否序列
- 填入对应账号的数据列（A/C/E/G/I/K/M），从第2行开始，**只填到第101行**
- 第102行及以后保持空白，不要填数据
- 密度公式列（B/D/F/H/J/L/N）自动计算，公式范围精确到第101行

**最近300条密度统计（倒数第1个tab）：**

- 提取前 **300条** feed视频的是/否序列（不足300条则填实际数量）
- 填入对应账号的数据列，从第2行开始，**只填到第301行**
- 第302行及以后保持空白

**列与账号的对应关系（每两列一组）：**

| 列对 | 账号位置 |
|------|----------|
| A-B | 第1个账号 |
| C-D | 第2个账号 |
| E-F | 第3个账号 |
| G-H | 第4个账号 |
| I-J | 第5个账号 |
| K-L | 第6个账号 |
| M-N | 第7个账号（我们的账号） |

#### 4.5.4 密度公式（必须带IF空值守卫）

每个密度列（B/D/F/H/J/L/N）的公式必须包含 `IF(COUNTA(...)=0,"",...)` 守卫，确保数据列为空时密度单元格也为空，不显示0%或残留值。

**100密度表公式（以N列第2行为例）：**

```
=IF(COUNTA(M$2:M2)=0,"",COUNTIF(INDIRECT("M"&MAX(2,ROW()-99)&":M"&ROW()),"是")/MIN(COUNTA(M$2:M2),100))
```

**300密度表公式（以N列第2行为例）：**

```
=IF(COUNTA(M$2:M2)=0,"",COUNTIF(INDIRECT("M"&MAX(2,ROW()-299)&":M"&ROW()),"是")/MIN(COUNTA(M$2:M2),300))
```

公式向下填充至数据末尾行（100密度→第101行，300密度→第301行），**不要预填到801行**。

> ⚠️ 缺少IF守卫会导致数据清空后密度列仍显示0%或上一次的值，造成视觉混乱。这是0824表踩过的坑。

#### 4.5.5 下拉列表数据验证

所有数据列（A/C/E/G/I/K/M）的第2行至数据末尾行，必须设置"是/否"下拉列表：

- 类型：list
- 选项：`["是", "否"]`
- 高亮：开启
- 颜色：是=`#bacefd`（蓝色），否=`#fed4a4`（橙色）

新增账号列时，先在第2行设置data_validation，再用copy-to-range批量应用到整列数据范围。**不要用copy-to-range覆盖已有值**——先读取现有值，再带值+验证写回。

#### 4.5.6 汇总表（时长&初步统计）

汇总表每行对应一个账号，列定义：

| 列 | 含义 | 填写方式 |
|----|------|----------|
| A | 账号ID + 画风分组 | 手动填写 |
| B | 分组（base/v1/v2/v3等） | 手动填写（如有） |
| C | 时间段 | 手动填写 |
| D | 时长 | 手动填写 |
| E | 人效（数量/小时） | 手动填写 |
| F | 总违规 | **仅feed违规数**（不含搜索） |
| G | 总消费 | **仅feed条数**（不含搜索） |
| H | 总违规/总消费 | F/G，可填公式或手动值 |
| I | 最近100条违规密度极值 | MAX(100密度表!对应密度列) |
| J | 最近300条密度极值 | MAX(300密度表!对应密度列) |

> **关键**：汇总表的"总违规"和"总消费"只统计feed，不包含搜索词消费。搜索视频命中率接近100%，混入后会导致违规率失真。

汇总表的密度极值可直接引用密度表公式列的MAX值，也可手动填写最终值。

#### 4.5.7 主数据表的FYF密度列

原始数据表的H列（FYF密度）只需在 **H2单元格** 填写当天的feed整体违规率（feed违规数/feed总数），不需要逐行或按100条里程碑填写。

例如：feed 317条，违规137条 → H2填 `43.2%`。

#### 4.5.8 操作检查清单

每日密度统计完成后，逐项确认：

- [ ] 100密度表：数据列只到第101行，102行以后为空
- [ ] 300密度表：数据列只到第301行，302行以后为空
- [ ] 密度公式包含IF空值守卫，数据清空后密度列也为空
- [ ] 公式只延伸到数据末尾行，没有预填到801行
- [ ] 所有数据列有"是/否"下拉验证（蓝色"是"/橙色"否"）
- [ ] 汇总表总违规/总消费只统计feed，不含搜索
- [ ] 主数据表H2填写了feed整体违规率
- [ ] 填入的是/否序列按时间顺序，且全部来自"刷feed"行为

---

## 五、常见问题排查

| 问题 | 原因 | 解决方法 |
|------|------|----------|
| 点击按钮报 `-10004` 辅助功能错误 | 运行环境无辅助功能权限 | 推荐双击 `启动Web控制台.command` 在 Terminal 中启动；如从Trae终端启动，需在系统设置→隐私与安全性→辅助功能中勾选Trae |
| 点击无反应/状态一直是忙碌 | 上一次宏卡住了 | 访问 http://127.0.0.1:8880/api/status 检查状态，必要时在Terminal按 Ctrl+C 重启 |
| 剪贴板没抓到新链接 | TikTok复制链接延迟 / 分享面板没弹出 | 手动在TikTok点分享→复制链接，观察控制台是否捕获 |
| 飞书写入报错 `specify at least one of --spreadsheet-token` | config.json的token被重置 | 检查 config.json 中 sheet.spreadsheet_token 和 sheet_id 是否正确，重启服务 |
| 飞书写入超时（error_code=1204） | 网络波动 | 控制台内置3次自动重试（指数退避），如持续失败检查lark-cli登录状态 |
| 点击位置偏移/点错按钮 | 镜像窗口位置变了 / 坐标校准丢失 | 在主控制台点击「复位窗口」或检查 config.json 中 profiles.main 坐标 |
| 互动列显示纯文本而非彩色标签 | 写入格式错误 | 确认使用 cells-set + multiple_values 格式（代码已处理，不应出现） |
| 链接列显示红色三角/长URL换行 | 写入冲突/格式丢失 | 确认链接写入为 rich_text 超链接格式 |
| F列(policy)内容为空 | category_id传错或has_policy_column=false | 确认按1/2/3键而非旧的0/1/2/3键 |
| 按1/2/3键无反应 | 全局热键未启动或控制台处于暂停状态 | 检查控制台是否已点「开始养号」，全局快捷键进程是否在运行 |
| 数据写到了错误的行（如从346行开始） | 换表后current_row未重置 | 修改config后通过API `{"action":"row:2"}` 重置行号；空表必须从第2行开始 |
| 三分类变二分类（按键含义错误） | 上一个NSA会话的binary_mode=true残留 | 修改config.json中binary_mode=false和has_policy_column=true后**重启控制台** |
| 自动按键发到了浏览器而非TikTok | 旧版auto_key未激活镜像窗口 | 确认使用更新版auto_key_web.py（每次按键前自动activate iPhone Mirroring）；或手动保持镜像在前台 |

---

## 六、自动按键工具（auto_key_web.py）

辅助工具：模拟键盘按键自动连按，用于需要定时按键的场景（如持续刷feed）。

### 6.1 启动方式

自动按键工具会随 `启动Web控制台.command` 自动启动（后台运行），无需单独启动。

如需单独启动：

```bash
cd /Users/bytedance/Documents/trae_projects/tt1/tiktok-ios-v2-large-console
python3 auto_key_web.py
```

启动后浏览器打开：http://127.0.0.1:8866/

### 6.2 功能说明

| 功能 | 说明 |
|------|------|
| ▶ 开始 | 开始自动按键（当前选中的1或2键），默认3秒一次 |
| ⏸ 暂停/▶ 继续 | 暂停或继续按键（也可按空格键） |
| ■ 停止 | 完全停止按键循环 |
| 1/2键选择 | 切换当前自动按的键；点击数字按钮也会立即按一次对应键 |
| ◧ 悬浮小窗 | 弹出280×220迷你窗口（3按钮：1=L1, 2=L2, 3=不违规中前两个），拖到角落 |
| 间隔调节 | 支持0.1s~3600s，预设0.5s/1s/2s/3s/5s/10s |
| 实时统计 | 显示本次按键次数、累计次数、运行时长 |

### 6.3 注意事项

- **自动激活iPhone镜像**：每次按键前工具会自动将iPhone Mirroring切到前台（`activateWithOptions`），然后等待50ms再发键，不需要手动保持镜像在前台。但首次使用前确认iPhone Mirroring已启动并连接
- 需要**辅助功能权限**：系统设置→隐私与安全性→辅助功能，勾选终端/Trae
- Ctrl+C 终止服务
- 自动按键仅模拟按1/2键，分类和写入仍由主控制台(8880)处理
- **不要同时操作两个工具**：自动按键运行时不要手动按主控制台的1/2/3键，避免并发写入冲突
- **间隔不要太短**：主控制台执行宏（点赞→收藏→转发→复制链接→多次观看）需要1-2秒，自动按键间隔建议≥3秒，否则上一条宏未完成就触发下一条
- **页面模式要匹配**：自动按键只发键盘事件，不区分feed/search模式。确保主控制台的page_mode与当前TikTok页面一致（feed流页面用feed模式的宏，搜索结果页用search模式的宏）
- 如果按键无反应，检查：①iPhone Mirroring是否已连接 ②主控制台是否在运行且非暂停 ③辅助功能权限是否已授权
- API控制（可用curl远程操作）：
  - 启动：`curl -X POST http://127.0.0.1:8866/api/start -H "Content-Type: application/json" -d '{"key":"1"}'`
  - 停止：`curl -X POST http://127.0.0.1:8866/api/stop`
  - 状态：`curl http://127.0.0.1:8866/api/status`

---

## 七、相关文件路径

```
/Users/bytedance/Documents/trae_projects/tt1/tiktok-ios-v2-large-console/
├── 启动Web控制台.command      ← 双击启动主控制台（必须在Terminal中运行）
├── 启动自动按键.command        ← 双击启动自动按键工具
├── config.json                ← 配置（token/sheet_id/端口/坐标/宏）
├── web_console.py             ← 主控制台后端（端口8880）
├── auto_key_web.py            ← 自动按键Web控制台（端口8866）
├── ios_sheet_writer.py        ← 飞书写入模块（含L1/L2 policy逻辑）
├── global_hotkeys.py          ← 全局快捷键守护进程（1/2/3键全局监听）
├── reset_iphone_mirroring.py  ← 窗口读取+鼠标宏执行
├── web/
│   ├── index.html             ← 主控制台页面
│   ├── mini.html              ← 悬浮小窗（3按钮）
│   ├── app.js / mini.js       ← 前端逻辑
│   └── style.css / mini.css   ← 样式
└── 养号密度统计SOP.md          ← 本文档
```
