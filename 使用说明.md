# iOS V2 大屏版使用 / 迁移说明

## 打包方式

把整个 `iOS V2` 文件夹压缩后发给同事，不要只发单个脚本。

建议同事解压到桌面：

```text
~/Desktop/iOS V2
```

## 最省事的运行方式

直接双击：

```text
一键准备并启动.command
```

它会依次处理：

- 编译鼠标宏组件
- 检查 / 安装提示 `lark-cli`
- 检查 / 引导飞书授权
- 提示辅助功能权限
- 启动 Web 控制台

浏览器会打开：

```text
http://127.0.0.1:8878/
```

这个地址是每台电脑自己的本地地址，不是公网链接。

## 分步脚本

如果一键脚本遇到问题，可以分开跑：

- `检查环境.command`：只检查环境，不启动控制台
- `飞书授权.command`：只处理飞书授权
- `启动Web控制台.command`：直接启动控制台

## 飞书授权

飞书写入权限取决于同事电脑上的 `lark-cli` 和同事当前授权的飞书账号。

即使表格已经共享给同事，同事电脑第一次写表通常仍需要授权一次。只要同事账号对表格有编辑权限，授权后就可以写入同一张表。

双击 `飞书授权.command` 会先检测当前账号能不能读这张表；不能读时会打开飞书授权链接。授权完成后回到终端窗口按回车，它会自动复测。

如果提示找不到 `lark-cli`，可以让一键脚本自动执行，或手动安装：

```bash
npm install -g @larksuite/cli
```

## 运行前确认

- iPhone Mirroring 能正常打开
- 先把 iPhone Mirroring 调到舒服的大屏尺寸
- 点“保存为主屏位”或“保存为外接屏位”
- 点“复位到主屏”后，窗口位置和大小稳定
- 控制台的“当前写入行”是准备开始写入的行
- V2 默认没有录制宏，需要先录日常分享版 / 转发版宏
- 先用测试行跑 3-5 条，确认无误后再正式跑

## 如果点不动

去 macOS：

```text
系统设置 -> 隐私与安全性 -> 辅助功能
```

允许 Terminal、Python 或启动器控制电脑。

## 正式保留文件说明

- `一键准备并启动.command`：推荐入口
- `启动Web控制台.command`：已准备好时直接启动
- `检查环境.command`：环境检查
- `飞书授权.command`：飞书授权
- `config.json`：窗口坐标、宏、表格行号配置
- `web/`：网页控制台前端
- `web_console.py`：网页控制台后端
- `reset_iphone_mirroring.py`：窗口复位、宏播放/录制
- `ios_sheet_writer.py`：飞书写入
- `mac_click*` / `mac_record*`：鼠标点击和录制组件
- `cat.png` / `cat-repost.png`：猫图
